# Run the init-config bashio script in a contenv environment; a non-zero exit
# from the script aborts startup (bashio::exit.nok surfaces an actionable error).
execlineb -P /etc/s6-overlay/scripts/init-config
